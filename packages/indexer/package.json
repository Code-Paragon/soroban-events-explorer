{
  "name": "@soroban-forge/indexer",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch & node dist/index.js",
    "start": "npx prisma db push --schema=../db/prisma/schema.prisma && node dist/index.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.1.0",
    "@soroban-forge/db": "*"
  }
}
